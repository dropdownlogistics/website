export const metadata = { title: 'About — Operator Profile — DDL' };

export default function Page() {
  return (
    <iframe
      src="/preview/operator-profile.html"
      style={{ width: '100%', height: '100vh', border: 'none' }}
      title="About — Operator Profile"
    />
  );
}
